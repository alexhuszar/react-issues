/**
 * Log error to the server
 * @param {Object} error
 * @param {Object.message} error
 * @param {Object.title} error
 * @param {String} info
 */
export const logError = (error, info) => {
  console.log(error, info)
};
